"use client";

const page = ({ProjectId}) => {
  return (
    <>
      <div>
        <h1 className="">Page No. {ProjectId}</h1>
        <h1 className="">Page No. {ProjectId}</h1>
        <h1 className="">Page No. {ProjectId}</h1>
        <h1 className="">Page No. {ProjectId}</h1>

    </div>
    </>
  )
}

export default page
